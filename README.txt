After running the script:
   Any added users and their account info should show up
   in accountInfo.txt as lastname;firstname;uh-username;password;date-added.

   Any errors will show up in errors.txt.

   Messages in the database that were processed get flagged to prevent
   them from being processed again.